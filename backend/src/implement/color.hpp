#pragma once

struct Color {
    float r, g, b, a; /// rgba
    Color(float r=0, float g=0, float b=0, float a=1) : r(r), g(g), b(b), a(a) {}
};

/**
 * r =  RED
 * g = GREEN
 * b = BLUE
 * a = ALPHA
 */

